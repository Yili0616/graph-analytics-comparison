library(igraph)

start_time <- Sys.time()
invisible(sample_pa(1000000, power = 1, m = 10, out.dist = NULL, out.seq = NULL,
  out.pref = FALSE, zero.appeal = 1, directed = TRUE,
  algorithm = c("psumtree", "psumtree-multiple", "bag"), start.graph = NULL))
end_time <- Sys.time()
end_time - start_time

start_time <- Sys.time()
invisible(sample_pa(1000000, power = 1, m = 10, out.dist = NULL, out.seq = NULL,
  out.pref = FALSE, zero.appeal = 1, directed = TRUE,
  algorithm = c("psumtree", "psumtree-multiple", "bag"), start.graph = NULL))
end_time <- Sys.time()
end_time - start_time

start_time <- Sys.time()
invisible(sample_pa(1000000, power = 1, m = 10, out.dist = NULL, out.seq = NULL,
  out.pref = FALSE, zero.appeal = 1, directed = TRUE,
  algorithm = c("psumtree", "psumtree-multiple", "bag"), start.graph = NULL))
end_time <- Sys.time()
end_time - start_time

start_time <- Sys.time()
invisible(sample_pa(1000000, power = 1, m = 10, out.dist = NULL, out.seq = NULL,
  out.pref = FALSE, zero.appeal = 1, directed = TRUE,
  algorithm = c("psumtree", "psumtree-multiple", "bag"), start.graph = NULL))
end_time <- Sys.time()
end_time - start_time

start_time <- Sys.time()
invisible(sample_pa(1000000, power = 1, m = 10, out.dist = NULL, out.seq = NULL,
  out.pref = FALSE, zero.appeal = 1, directed = TRUE,
  algorithm = c("psumtree", "psumtree-multiple", "bag"), start.graph = NULL))
end_time <- Sys.time()
end_time - start_time

